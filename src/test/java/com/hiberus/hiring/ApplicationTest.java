package com.hiberus.hiring;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

/**
 * Basic unit test for the Application class.
 * This test ensures that the Spring Application Context loads without errors.
 */
@SpringBootTest
class ApplicationTest {

    @Test
    void contextLoads() {
        // If the application context fails to load, this test will fail.
    }
}